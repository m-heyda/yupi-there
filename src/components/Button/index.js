import React, { Component } from 'react';

class Button extends Component {
  render() {
    return (
      <React.Fragment>
        <button>
          Button
        </button>
      </React.Fragment>
    );
  }
}

export default Button;
